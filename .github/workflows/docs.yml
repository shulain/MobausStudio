# 触发公开仓库部署文档站
# 当私有仓库 user-guide/ 有变更时，触发公开仓库执行文档构建和部署
#

name: Trigger Docs Deploy

on:
  push:
    branches: [main]
    paths:
      - "user-guide/**"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: 触发公开仓库部署文档
        env:
          PAT: ${{ secrets.PUBLIC_REPO_PAT }}
          PUBLIC_REPO: ${{ secrets.PUBLIC_REPO }}
        run: |
          echo "🚀 触发公开仓库部署文档站"

          curl -sS -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $PAT" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${PUBLIC_REPO}/dispatches" \
            -d '{"event_type":"deploy-docs"}'

          echo "✅ 已触发文档部署"
